# Vendored Hugo reference site

These `layouts/` and `config/_default/config.yaml` are vendored verbatim from
`linden-project/linny-notebook-template` — the reference Hugo indexer ("Carl").
They are the golden reference `lindexer verify --hugo` builds and diffs our index
against. Update them when the upstream template's index emission changes.
