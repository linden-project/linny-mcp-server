// Package config loads server configuration, including the notebook path(s),
// state dir, listen address, and the path to the bearer-token file. It is
// designed for N notebooks even when v1 serves one.
package config
