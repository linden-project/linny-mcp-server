# add-typed-front-matter-values

Typed front-matter writes: YAML sequences and real scalars instead of stringified
values, plus idempotent `add_term` / `remove_term` so an agent can classify a document.
