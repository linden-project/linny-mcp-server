# Documentation

| Document | Status | Purpose |
|--------------------------|-------------|--------------------------------------------|
| `linden-index-spec.md`   | next epic   | The Linden Index Specification (v0.3). Written *before* the indexer code; a competent implementer could write a conforming indexer from it alone. |
| `tools.md`               | milestone 5 | The versioned MCP tool surface. Tool names are an API. |
| `future.md`              | milestone 6 | Out-of-scope work: semantic YAML git merge driver, local embeddings, mipnix wiring. |

See `openspec/` for the change proposals that produce each of these.
